services:
  mongo:
    ports:
      - "27017:27017"

  backend:
    ports:
      - "8080:8080"
    environment:
      SLACK_ERROR_TOKEN: ${SLACK_ERROR_TOKEN:-}

  frontend:
    ports:
      - "3000:80"
